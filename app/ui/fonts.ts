import { Chakra_Petch, Inter, Lusitana } from 'next/font/google';

export const inter = Inter({subsets: ['latin']});
export const lusitana = Lusitana({subsets: ['latin'], weight: ['400','700']});
export const raleway = Chakra_Petch({subsets:['vietnamese'], weight:['400']})